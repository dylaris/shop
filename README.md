# shop

A simple cmdline flag parser in C99. Only support short option, like '-h'.

## Quick Start

```console
$ make
$ ./example -h
$ ./example -v -n 42 -f data.txt -b true -d 3.14
$ ./example -vn 42 -fdata.txt -b1 -d2.5
$ ./example -vf data.txt
```
